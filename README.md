#### Tasks

1. **Cookies**: Complete the `/api/login` route to set a secure `session_id` cookie and verify it in `/api/check-session`.
2. **Local Storage**: Finish the `SavePrefs` component to save and display a user’s post filter preference.
3. **Session Storage**: Complete the `Draft` component to save and retrieve a draft specific to the current tab.
4. **IndexedDB**: Implement the `saveDraft` and `loadDrafts` functions in `draftsDB.ts` to store and display multiple drafts.
